//string values
const str1 = 'who';
const str2 = 'dis';

//this is result in both strings are equal
// const str3 = '?';
// if (str3 === str2) alert('Both strings are equal');

//comparing the strings and showing alert
if (str1 === str2) alert('Both strings are equal');
else alert('both strings are not equal');
